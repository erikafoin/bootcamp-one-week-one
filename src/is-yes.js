export const isYes = (someString) => {
    if (someString === 'yes') {
        return true;
    } else {
        return false;
    }
};

export const isNo = (someString) => {
    if (someString === 'no') {
        return true;
    } else {
        return false;
    }
};
